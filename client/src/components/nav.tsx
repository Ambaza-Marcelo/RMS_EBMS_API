import { HamburgerMenuIcon } from "./ui/hamburger-menu-icon";

export const Nav = () => {
    return (
        <nav className="flex ml-auto">
            <HamburgerMenuIcon />
        </nav>
    );
};
